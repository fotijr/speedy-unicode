<Window x:Class="SpeedyUnicode.UnicodeSelection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpeedyUnicode"
        mc:Ignorable="d"
        Title="Speedy Unicode" Height="350" Width="525" IsVisibleChanged="Window_IsVisibleChanged" WindowStartupLocation="CenterScreen">
    <DockPanel Margin="10">
        <TextBox DockPanel.Dock="Top" Margin="0,0,0,10" Name="SearchText" KeyUp="SearchText_KeyUp" TabIndex="0" PreviewKeyDown="SearchText_KeyDown" />
        <ListView Name="UnicodeListView" PreviewMouseLeftButtonUp="UnicodeListView_PreviewMouseLeftButtonUp" FontSize="16" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="100" Header="" DisplayMemberBinding="{Binding Unicode}" />
                    <GridViewColumn Width="325" Header="Name" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Width="60" Header="#" DisplayMemberBinding="{Binding FilterAccuracy}" />
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>

</Window>
